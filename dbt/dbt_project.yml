name: airports_v2
version: "2.0.0"
profile: airports_v2

model-paths: ["models"]
macro-paths: ["macros"]

models:
  airports_v2:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts
