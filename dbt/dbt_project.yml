name: airports_v2
version: "2.0.0"
profile: airports_v2

flags:
  send_anonymous_usage_stats: false

model-paths: ["models"]
macro-paths: ["macros"]

models:
  airports_v2:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts
